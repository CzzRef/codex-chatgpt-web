# Changes

2026-09-08: delivered V0.1 in paired isolated worktrees, with no installed application or real account route activation.

Codex++ adds opt-in unified settings, scoped config/Interrupt recovery, native context and model-cache ownership, a secret-free manifest, explicit API execution with revision snapshots, request cancellation, separate component supervision and lifecycle protections for legacy entry points. Its actual injected settings panel provides component import, Web connection, per-task reasoning and aggregate member/default controls.

CCW adds the externally owned gateway entry and a private Electron profile, persistent per-group/per-turn routing, next-request effort controls, tools-disabled API compaction for both wire contracts, native identity/stream preservation, and independent Web configuration/drain handling. Standalone setup, route, update and uninstall ownership remain protected in managed mode.

Packaging pins Bun and Electron, preserves framework links, carries licenses and supplies a disposable macOS smoke check. Focused tests cover both new behavior and affected legacy paths. Product architecture, security, trial instructions, offline preview and worktree records are synchronized.

See [verification](verify.md) for counts and the real account gates that remain open. The user requested local batched submission and will test the target account later. The implementation commits below are followed by the local documentation closeout commit; no target integration or push is included.

## Local implementation commits

| Commit | Scope |
| --- | --- |
| `b6f7522` | 任务轮次绑定的统一 API 路由 |
| `592d00d` | Codex++ 托管入口与私有 Web 运行环境 |
