# Plan

1. P0: freeze contracts, isolated fixtures, ownership and requirement mapping.
2. P1: opt-in unified settings, scoped config transaction/recovery and legacy writer guards.
3. P2: merged catalog, per-request explicit API targets, official passthrough and identity propagation.
4. P3: tool and streaming behavior, API compaction v1/v2 and unknown-history rejection.
5. P4: per-group/per-turn routing, failure boundaries and live next-request effort controls.
6. P5: managed Web gateway/browser lifecycle, unified control page, versioned packaging.
7. P6: focused automated checks, product documentation and explicit real macOS acceptance record.

All code writes belong to App Root. Independent read-only source mapping completed for CCW lifecycle and CPP UI. Preserve original checkouts except the worktree observation ledger. Retain worktrees until requested integration/cleanup.
